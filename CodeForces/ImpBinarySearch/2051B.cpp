#include<iostream>
#include<cstring>
#include<climits>
#include<ctype.h>
#include<cmath>
using namespace std;

int n, a , b, c;

int main(){
    int t;
    cin >> t;
    while (t--)
    {
        cin >> n >> a >> b >> c;
        int day =1;
        int covered = 0;
        while(covered<=n){
            if(day);
        }

    }
    
    return 0;
}